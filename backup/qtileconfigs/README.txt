Standard file name of Qtile config is:
"config.py"

Name is preceded by version and will be formatted as:
"conf1_config.py"
You simply have to remove the "conf1_" and save into folder as "config.py"

Path for qtile config is:
"/home/danny/.config/qtile/config.py"
